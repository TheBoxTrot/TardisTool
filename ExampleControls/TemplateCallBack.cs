using Sandbox;

public partial class TemplateCallBack : BaseCallBack
{
	//for best practice just use the same name as the class
	public override string CallBackName { get; protected set; } = "TemplateCallBack";
}
